<%= form_with(model: [:app, domain], class: "space-y-6") do |form| %>
  <% if domain.errors.any? %>
    <div class="bg-red-900/20 backdrop-blur-sm rounded-lg p-6 text-white border border-red-500/30 shadow-[0_0_30px_rgba(239,68,68,0.1)]">
      <div class="flex items-center mb-3">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-red-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
        </svg>
        <h2 class="font-semibold text-red-200"><%= pluralize(domain.errors.count, "error") %> prohibited this domain from being saved:</h2>
      </div>
      <ul class="list-disc list-inside text-red-200 space-y-1">
        <% domain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, "Domain Name", class: "block text-sm font-semibold text-yellow-400 mb-3" %>
    <%= form.text_field :name, 
        placeholder: "example.com", 
        class: "block w-full px-4 py-3 rounded-lg bg-[#0F172A]/60 border border-yellow-500/20 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500/40 focus:border-yellow-500/40 transition-all duration-200" %>
    <p class="mt-2 text-sm text-gray-400">
      Enter the domain name without protocol (e.g., example.com)
    </p>
  </div>

  <div>
    <%= form.label :country, "Country Code", class: "block text-sm font-semibold text-yellow-400 mb-3" %>
    <%= form.text_field :country, 
        placeholder: "us", 
        class: "block w-full px-4 py-3 rounded-lg bg-[#0F172A]/60 border border-yellow-500/20 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500/40 focus:border-yellow-500/40 transition-all duration-200" %>
    <p class="mt-2 text-sm text-gray-400">
      Enter the two-letter country code for localized search results (e.g., us, uk, de)
    </p>
  </div>

  <div>
    <%= form.label :tracked_keywords_count, "Tracked Keywords Count", class: "block text-sm font-semibold text-yellow-400 mb-3" %>
    <%= form.number_field :tracked_keywords_count,
        placeholder: "10",
        step: 1,
        class: "block w-full px-4 py-3 rounded-lg bg-[#0F172A]/60 border border-yellow-500/20 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500/40 focus:border-yellow-500/40 transition-all duration-200" %>
    <p class="mt-2 text-sm text-gray-400">
      Enter the number of keywords to track for this domain
    </p>
  </div>


  <div class="pt-4">
    <%= form.submit class: "group relative px-8 py-3 bg-gradient-to-r from-yellow-500 to-red-500 text-white rounded-md font-semibold transition-all duration-200 hover:shadow-[0_0_20px_rgba(234,179,8,0.3)] cursor-pointer inline-flex items-center" %>
  </div>
<% end %>
